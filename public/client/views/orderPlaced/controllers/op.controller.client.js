(function () {
    angular
        .module("WebAppMaker")
        .controller("OpController", OpController);

    function OpController($rootScope, $location) {
        var vm = this;

        function init() {
        }
        init();

    }
})();
